# 图片百度收录优化说明

## 一、本次已完成的代码优化

### 1. 修复 JSON-LD 结构化数据路径错误（关键）

所有 78 个文章页和首页的 `ImageObject` 结构化数据中，图片地址原本是重复路径：

```
错误: https://www.shunar.cn/zb_users/upload/2026-08/zb_users/upload/2026-08/xxx.jpg
正确: https://www.shunar.cn/zb_users/upload/2026-08/xxx.jpg
```

这个 bug 会导致百度无法抓取到真实图片文件，是图片难以收录的直接原因，现已全部修正。

### 2. 图片 alt / title 差异化

原站 1000+ 处图片几乎全部使用 `alt="金刚石选型机"`，与图片实际内容不符。
现已按每张图片的真实内容差异化命名，例如：

- `zirconia-bead-separator.jpg` → alt="氧化锆球分离机"

- `diamond-shape-sorting-machine.jpg` → alt="金刚石形状分选机"

- `micro-beads-001-10mm.jpg` → alt="金刚石微粉10mm颗粒"

- `ceramic-bead-screening-machine.jpg` → alt="陶瓷微珠筛选机"

### 3. 图片地址改为绝对域名 URL

原页面使用相对路径 `../zb_users/upload/...`，现统一改为
`https://www.shunar.cn/zb_users/upload/...`，并同步修正了正文中的 `mip-img` 标签。

### 4. 重建 sitemap-images.xml（图片 sitemap）

- 覆盖全部 11 张上传图片（本次补齐了原先缺失的 4 张陶瓷类图片）

- 为每张图片补充了唯一的 `<image:title>` 和 `<image:caption>`

- 首页 + 各文章页图片均正确关联

- 共 253 条图片记录

### 5. robots.txt 校验

已确认允许百度抓取 `/zb_users/upload/` 图片目录，并正确声明了两个 sitemap。

***

## 二、你需要手动完成的操作（百度站长平台）

上面的代码优化让图片"具备了被收录的条件"，但要让百度真正收录，
还需要在百度搜索资源平台完成以下操作：

### 1. 验证站点

- 访问 <https://ziyuan.baidu.com/> 登录，添加站点 `www.shunar.cn`

- 选择文件验证 / HTML 标签验证 / CNAME 验证任选其一

### 2. 提交 sitemap

- 站点管理 → 普通收录 → sitemap

- 提交以下两个地址：

  - `https://www.shunar.cn/sitemap.xml`

  - `https://www.shunar.cn/sitemap-images.xml`

### 3. 图片收录提交

- 站点管理 → 图片收录查询/提交，提交图片 sitemap

- 百度收录图片主要基于 百度图片 与 网页正文图片 两个维度，
  图片 sitemap 提交后建议再主动提交文章页 URL

### 4. 主动推送文章页

- 每新增一篇文章，使用"普通收录 → 快速收录/普通收录-页面推送"提交
  该文章 URL（因为文章页是图片的载体）

***

## 三、长期维护建议

1. **图片命名**：上传图片前用英文关键词命名（如 `diamond-sorting-machine.jpg`），
   百度能更好地识别主题；本目录图片命名已符合规范。
2. **图片尺寸**：建议单图宽 ≥ 700px、小于 1MB，清晰度越高越有利。
3. **每页配图**：每篇文章至少配 1-3 张与内容完全一致的产品实拍图，
   避免用无关配图（alt 与图片内容不符会影响信任度）。
4. **新文章 alt 规范**：写文章时，图片 alt 用"产品名+属性"，不要全站雷同。
5. **定期提交**：站点有新增内容后，重新在站长平台提交 sitemap。

***

## 四、本次修改涉及的文件

- 根目录及 `post/` 下全部 134 个 HTML 文件：
  图片 src 绝对化、alt/title 差异化、JSON-LD 路径修复

- `sitemap-images.xml`：重建（唯一图片 title/caption + 补齐陶瓷图）

- `robots.txt`：未改（已满足要求）

- 新增脚本 `tools/fix_images_seo.py`：可重复执行批量修复

